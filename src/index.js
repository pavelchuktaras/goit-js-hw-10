import { fetchBreeds } from './cat-api.js';

const select = document.querySelector('.breed-select');

// select.addEventListener('input', inputCheck);

// function inputCheck(evt) {
//   evt.preventDefault();
//   fetchBreeds(evt).then(({ name }) => console.log(name));
// }
